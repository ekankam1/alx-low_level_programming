#include "main.h"
/**
 * _isupper - checks for uppercase character
 *
 * @c: input should be an integer
 *
 * Return: 1 if character is uppercase otherwise 0
 */
int _isupper(int c);
{
int c;
while ((c >= 65) && (c <= 90))
{
	return (1);
}
return (0);
}
