#include "main.h"
/**
 * _abs - compute absolute value of int
 *
 * @x: input integer
 *
 * Return: returns the absolute value
 */
int _abs(int x)
{
	if (x < 0)
	{
		x *= -1;
		return (x);
	}
		else
		{
			return (x);
		}
}
